spring:
  datasource:
    url: jdbc:postgresql://localhost:15432/shop_demo
    username: shop
    password: shop
  redis:
    host: localhost
    port: 16379
  flyway:
    enabled: true

app:
  mode: demo

benchmark:
  iterations: 10000
  warmupIterations: 1000
  resultFile: build/reports/benchmark-results.csv
